version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 16
  build:
    commands:
      - echo "Build started on `date`"
  post_build:
    commands:
      - echo "Build finished on `date`"
artifacts:
  files:
    - '**/*'
